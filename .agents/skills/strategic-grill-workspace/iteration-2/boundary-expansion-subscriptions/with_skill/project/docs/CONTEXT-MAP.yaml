contexts:
  - name: User
    path: ./packages/user/docs/CONTEXT.yaml
    tag: generic
    description: Identity, authentication and session management; using an off-the-shelf identity provider.

  - name: Billing
    path: ./packages/billing/docs/CONTEXT.yaml
    tag: supporting
    description: Generates invoices and processes payments.

relationships:
  - upstream: User
    downstream: Billing
    label: Customer-Supplier
    implication: User owns the UserId contract; Billing adapts to it.
